{
  "name": "open-cli-plugin-obs",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "test": "vitest run",
    "install-cli": "node esbuild.config.mjs production && chmod +x cli.js && mkdir -p ~/.local/bin && cp cli.js ~/.local/bin/obs-agent"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^25.3.0",
    "esbuild": "^0.27.3",
    "obsidian": "^1.12.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
